@model TABGra.Models.Serwer

@{
    ViewBag.Title = "Details";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Details</h2>

<div>
    <h4>Serwer</h4>
    <hr />
    <dl class="dl-horizontal">
        <dt>
            @Html.DisplayNameFor(model => model.nazwa)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.nazwa)
        </dd>
        <dt>
            @Html.DisplayNameFor(model => model.pojemnośc)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.pojemnośc)
        </dd>
        @{
            TABGra.Models.ApplicationDbContext db = new TABGra.Models.ApplicationDbContext();
            var admin = db.gracz.Find(Model.id_admina).nazwa;
        }
        <dt>
            @Html.ActionLink("Admin","Details", "Graczs", new { id = Model.id_admina },null)
        </dt>

        <dd>
            @Html.DisplayName(admin)
        </dd>
        <dt>
            @Html.ActionLink("Gracze", "SerwerGracze", "Graczs", new { id = Model.id }, null)
        </dt>

        <dd>
        </dd>
    </dl>
</div>
<p>
    @Html.ActionLink("Edit", "Edit", new { id = Model.id }) |
    @Html.ActionLink("Back to List", "Index")
</p>
